# OPTIONAL ML INSTALL:
#   pip install -r requirements-ml.txt
# Full dev + test + ML:
#   pip install -r requirements-dev.txt && pip install -r requirements-ml.txt

openai-whisper==20240930
torch==2.3.1
torchaudio==2.3.1
